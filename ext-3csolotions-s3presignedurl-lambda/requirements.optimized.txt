# Optimized Production Dependencies - Minimal Set
# Reduces image size by ~40MB and cold start by ~1-2 seconds

# Core Framework (Required)
fastapi==0.109.2
pydantic==2.6.1

# Lambda Integration (Required)
mangum==0.17.0

# AWS SDK - boto3 is pre-installed in Lambda environment!
# We only need to pin the version if we need specific features
# Uncomment only if you need newer boto3 features than Lambda provides
# boto3==1.34.52
# botocore==1.34.52

# REMOVED (not needed for Lambda deployment):
# ❌ uvicorn - only for local dev (FastAPI + Mangum doesn't need it)
# ❌ python-multipart - no file uploads in API
# ❌ email-validator - no email validation in models
